import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'MediTrust - Blockchain Prescription Verification',
  description: 'Blockchain-powered prescription verification system that ensures doctors, patients, and pharmacies can trust prescriptions by making them tamper-proof using Hedera and Supabase.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
